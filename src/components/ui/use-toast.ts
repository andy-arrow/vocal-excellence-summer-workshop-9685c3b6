
// Re-export the useToast hook and toast function from the hooks folder
import { useToast } from "@/hooks/use-toast";

// Re-export toast object from hooks folder
import { toast } from "@/hooks/use-toast";

export { useToast, toast };
