/**
 * TuitionHero — dead component.
 * The /tuition route redirects to /apply. This file is kept
 * to avoid orphaned-file noise, but nothing imports or renders it.
 */
const TuitionHero = () => null;
export default TuitionHero;
